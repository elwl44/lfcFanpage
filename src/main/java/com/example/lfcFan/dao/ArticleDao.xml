<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lfcFan.dao.ArticleDao">
	<select id="getForPrintArticles" resultMap="article">
		SELECT A.*,
		M.loginId AS extra__writer
		FROM article AS A
		INNER JOIN member
		AS M
		ON A.memberId = M.id
		WHERE 1
		<if test="search_target != 'title_content'.toString() and search_target != null and searchKeyword != null">
			AND ${search_target} LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="search_target == 'title_content'.toString()">
				and (title like CONCAT('%', #{searchKeyword}, '%')
					or body like CONCAT('%', #{searchKeyword}, '%'))
		</if>
		ORDER BY A.id DESC
		<if test="limitFrom != null and limitTake != null">
			LIMIT #{limitFrom}, #{limitTake}
		</if>
		
	</select>

	<select id="getTotalCount" resultType="int">
		SELECT COUNT(*)
		FROM article AS A
		INNER JOIN member
		AS M
		ON A.memberId = M.id
		WHERE 1
		<if test="search_target != 'title_content'.toString() and search_target != null and searchKeyword != null">
			AND ${search_target} LIKE CONCAT('%', #{searchKeyword}, '%')
		</if>
		<if test="search_target == 'title_content'.toString()">
				and (title like CONCAT('%', #{searchKeyword}, '%')
					or body like CONCAT('%', #{searchKeyword}, '%'))
		</if>
	</select>

	<insert id="writeArticle" useGeneratedKeys="true"
		keyProperty="id">
		INSERT INTO article
		<set>
			regDate = NOW(),
			updateDate = NOW(),
			<if test="title != null">
				title = #{title},
			</if>
			<if test="body != null">
				`body` = #{body},
			</if>
			<if test="memberId != null">
				`memberId` = #{memberId},
			</if>
			`reading` = 0
		</set>
	</insert>

	<select id="getForPrintArticleById" resultMap="article">
		SELECT A.*,
		M.loginId AS extra__writer
		FROM article AS A
		INNER JOIN `member` AS M
		ON A.memberId = M.id
		WHERE A.id = #{id}
	</select>

	<delete id="deleteArticleById">
		DELETE FROM article
		WHERE id = #{id}
	</delete>

	<update id="modifyArticle">
		UPDATE article
		<set>
			updateDate = NOW(),
			<if test="title != null">
				title = #{title},
			</if>
			<if test="body != null">
				`body` = #{body},
			</if>
		</set>
		WHERE id = #{id}
	</update>
	
	<resultMap type="Article" id="article">
		<id property="id" column="id" />
		<id property="regDate" column="regDate" />
		<id property="updateDate" column="updateDate" />
		<id property="memberId" column="memberId" />
		<id property="title" column="title" />
		<id property="body" column="body" />
		<id property="reading" column="reading" />

		<association property="extra" javaType="map">
			<id property="writer" column="extra__writer" />
		</association>
	</resultMap>
</mapper>